---
description: Default entry point — identical to /dex:debate.
argument-hint: "<task or question>"
---

This command is identical to `/dex:debate`. Read `${CLAUDE_PLUGIN_ROOT}/commands/debate.md` and follow its instructions exactly, using the following as the task:

$ARGUMENTS
